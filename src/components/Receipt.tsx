import { AppColors } from "../theme";

function Receipt({ children }: { children: React.ReactNode }) {
  return (
    <>
      <div className={"bg-onPrimary shadow-xl flex p-16"}></div>
    </>
  );
}

export default Receipt;
